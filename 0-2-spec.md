Version 0.2 Enhancements

The Currency bar should always be displayed
Reorder it to Gold>Silver>Copper

Add a Spins counter to the currency bar, increases each spin.  This is useful to compare how quickly players gets to the win condition.

Split the layout into tabs:
0 Reel Tab: Displays the contents of the player's reel
1 Spin Tab: Displays the slot machine and allows the user to press the spin button
2 Market Tab: Allows the user to buy icons from the market

With the new tab layout, there should be space to increase the size of the slot machine grid

Improve the animation of SPIN.  
  While in "spinning state"
    All columns should enter the spinning state immediately after pressing the SPIN button
    the displayed icons in the machine should change every 0.2 seconds
    Make it obvious that a column has stopped spinning
  When all columns are done spinning
    Display a modal informing the user of the result of the spin and earned currencies

Resize icon size to 128x128 px

Change the starting reel to 2 blanks, 1 apple, 1 copper 